# Changes

Human-readable daily log. See `CHANGELOG.md` for versioned release entries.

## 2026-09-11

v0.1.0 initial release: one `team-orchestrator` skill plus role configs covering 12 tools, with skills-only and full-install paths documented in `guides/quickstart.md`.

## 2026-09-14

Routing, verification, and CI work: static category routing (`quick` / `deep` / `ultrabrain` / `visual`), a `verify.py` check for generated outputs, and a CI step running the test plus verify gates.
